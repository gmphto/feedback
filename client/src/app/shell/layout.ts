/**
 * Shell-level constants.
 *
 * The rail brand block and the top bar are separate components that must end at
 * the same height, so the value lives here instead of in either one.
 */

/** Icon rail width. The rail stays collapsed; labels appear in tooltips. */
export const railWidth = 56;

/** Top bar height, matched by the rail brand block. */
export const topBarHeight = 48;

/** The product name. The top bar and the signed-out page both show it. */
export const productName = 'Project Scope Tool';
