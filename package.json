{
  "name": "project-scope-tool",
  "private": true,
  "version": "0.1.0",
  "packageManager": "pnpm@11.5.0",
  "engines": {
    "node": ">=22.13"
  },
  "scripts": {
    "dev": "pnpm --recursive --parallel --filter client --filter server run dev",
    "build": "pnpm -r build",
    "test": "pnpm --filter client test",
    "typecheck": "pnpm -r typecheck"
  }
}
